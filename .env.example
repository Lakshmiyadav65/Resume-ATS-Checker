# Groq API key (free tier) — enables live LLM rewrites/tailoring. Get one at
# https://console.groq.com/keys . Without it, the app uses deterministic fallbacks.
GROQ_API_KEY=
# Optional: override the model (default: llama-3.3-70b-versatile)
GROQ_MODEL=

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
